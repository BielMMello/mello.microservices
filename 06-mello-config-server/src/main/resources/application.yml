server:
  port: 8888
spring:
  application:
    name: mello-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BielMMello/melloservice-config-server
          #username: username
          #password: password
          default-label: main
          search-paths:
          - 'greeting-service*'